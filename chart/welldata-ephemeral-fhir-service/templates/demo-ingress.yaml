{{- if and .Values.demoClient.enabled .Values.demoClient.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "welldata.demoClient.name" . }}
  labels:
    {{- include "welldata.demoClient.labels" . | nindent 4 }}
  {{- with .Values.demoClient.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.demoClient.ingress.className }}
  ingressClassName: {{ .Values.demoClient.ingress.className }}
  {{- end }}
  {{- if .Values.demoClient.ingress.tls }}
  tls:
    {{- range .Values.demoClient.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.demoClient.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "welldata.demoClient.name" $ }}
                port:
                  number: {{ $.Values.demoClient.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
